# ==============================================
# PRODUCTION DEPLOYMENT ENVIRONMENT VARIABLES
# ==============================================

# CRITICAL: Required for production deployment
# Set these before deploying to avoid deployment failures

# Port Configuration (REQUIRED for production)
# The deployment service requires PORT to be explicitly set
PORT=5000

# Database Configuration (REQUIRED)
DATABASE_URL=your_database_url

# Authentication Configuration (REQUIRED for production)
# REPLIT_DOMAINS must be set to your deployed domain(s)
# Example: REPLIT_DOMAINS=yourapp.replit.app,yourapp.replit.dev
REPLIT_DOMAINS=

# Session Security (REQUIRED for production)
# Generate a secure random string for production
# Example: openssl rand -base64 32
SESSION_SECRET=

# Public Domain Configuration (REQUIRED for SCORM functionality)
# This MUST match your deployed domain for SCORM packages to work in external LMS
# Examples:
# PUBLIC_DOMAIN=https://your-custom-domain.com
# PUBLIC_DOMAIN=https://yourapp.replit.app
PUBLIC_DOMAIN=

# ==============================================
# REDIS CONFIGURATION
# ==============================================

# Redis Cache Configuration (OPTIONAL - graceful degradation if not set)
# If not set, sessions will fall back to PostgreSQL
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Cache TTL Configuration (OPTIONAL)
CACHE_TTL_DEFAULT=300

# ==============================================
# CDN STORAGE CONFIGURATION
# ==============================================

# Storage Provider Selection (OPTIONAL - defaults to local)
# Options: local | cloudflare-r2 | s3-cloudfront
STORAGE_PROVIDER=local

# CloudFlare R2 Configuration (Required if STORAGE_PROVIDER=cloudflare-r2)
# CloudFlare R2 provides S3-compatible object storage with automatic CDN
# Sign up at: https://www.cloudflare.com/products/r2/
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET_NAME=
CLOUDFLARE_R2_CDN_DOMAIN=

# CloudFlare Zone Configuration (Required for cache purging)
# Get these from your CloudFlare dashboard under the zone settings
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_API_TOKEN=

# AWS S3 + CloudFront Configuration (Required if STORAGE_PROVIDER=s3-cloudfront)
# AWS S3 for storage + CloudFront for CDN distribution
# Setup guide: https://aws.amazon.com/cloudfront/
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=
AWS_CLOUDFRONT_DOMAIN=
AWS_CLOUDFRONT_DISTRIBUTION_ID=

# Local Storage Configuration (OPTIONAL - used when STORAGE_PROVIDER=local)
# Directory where course files are stored locally
UPLOADS_DIR=

# ==============================================
# OPTIONAL/DEVELOPMENT SETTINGS
# ==============================================

# Environment Mode
NODE_ENV=production

# Replit-specific (automatically set by Replit platform)
REPLIT_DB_URL=
ISSUER_URL=https://replit.com/oidc
REPL_ID=

# Development Settings (not needed for production)
# NODE_ENV=development